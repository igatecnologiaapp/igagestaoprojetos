# Variáveis públicas do backend (expostas ao navegador por design)
VITE_SUPABASE_URL="https://SEU-PROJETO.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="chave-publica-anon"
VITE_SUPABASE_PROJECT_ID="seu-project-id"

# Variáveis de servidor (nunca devem ser expostas ao navegador)
SUPABASE_URL="https://SEU-PROJETO.supabase.co"
SUPABASE_PUBLISHABLE_KEY="chave-publica-anon"
SUPABASE_PROJECT_ID="seu-project-id"

# NUNCA versionar segredos reais (service role, tokens, senhas).
# Segredos de servidor devem ser cadastrados no cofre de segredos do backend.
